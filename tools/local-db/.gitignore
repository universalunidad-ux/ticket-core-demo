# Efímeros del harness local — no versionar.
.runtime/
.artifacts/

# Efímeros del simulacro de recuperación (run-recovery-v2.sh) — no versionar.
# .artifacts-recovery/ contiene 01_bootstrap.log, con las claves locales
# ANON_KEY/SERVICE_ROLE_KEY que emite `supabase start`: NUNCA debe ser committable.
.runtime-recovery/
.artifacts-recovery/

# Efímeros del smoke semántico de auth-seed.
.runtime-auth-seed-smoke/
.auth-seed-smoke.lock/
